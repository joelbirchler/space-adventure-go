var oOo = require('../koala.js');

module.exports = {

    keys: function(test) {
        test.done();
    },

    values: function(test) {
        test.done();
    },

    extend: function(test) {
        test.done();
    }

}