var oOo = require('../koala.js');

module.exports = {
    
    curry: function(test) {
        test.done();
    },

    curry2: function(test) {
        // test with second param of # of args
        test.done();
    },

    partial: function(test) {
        // use oOo.__ for blanks
        test.done();
    },

    testKoalaCurried1: function(test) {
        test.done();
    }
    
}