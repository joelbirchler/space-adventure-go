var oOo = require('../koala.js');

module.exports = {

    isType: function(test) {
        // type function with 2 params... everything else curries to this...
        test.done();
    },

    isArray: function(test) {
        test.done();
    },

    isObject: function(test) {
        test.done();
    },

    isFunction: function(test) {
        test.done();
    },

    isNumber: function(test) {
        test.done();
    },

    isBoolean: function(test) {
        test.done();
    },

    isDate: function(test) {
        test.done();
    },

    isRegExp: function(test) {
        test.done();
    },

    isUndefined: function(test) {
        test.done();
    },

    isNull: function(test) {
        test.done();
    },

    isNaN: function(test) {
        test.done();
    }

};