var oOo = require('../koala.js');

module.exports = {

    has: function(test) {
        test.done();
    },

    all: function(test) {
        test.done();
    },

    any: function(test) {
        test.done();
    },

};