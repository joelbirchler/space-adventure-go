var oOo = require('../koala.js');

module.exports = {

    select: function(test) {
        test.done();
    },

    reject: function(test) {
        test.done();
    },

    head: function(test) {
        test.done();
    },

    tail: function(test) {
        test.done();
    },

    find: function(test) {
        test.done();
    },

    without: function(test) {
        test.done();
    },

    unique: function(test) {
        test.done();
    },

    at: function(test) {
        test.done();
    }

};